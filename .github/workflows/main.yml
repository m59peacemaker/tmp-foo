name: CI

on: [push]

jobs:
  lint-fix:
    steps:
    - name: prettier
      uses: bltavares/actions/prettier@master
      #env:
        #GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }
      with:
        args: 'autofix'
