name: CI

on: [push]

jobs:
  lint-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: prettier
        uses: m59peacemaker/actions/prettier@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
